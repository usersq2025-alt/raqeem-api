<?php

// TODO: قرار مؤقت يحتاج تأكيد فريق المحتوى.
// الوثيقة التصميمية الأصلية تنص حرفيًا أن صيغة عتبات البطارية "تُحدَّد بالتنسيق
// مع فريق المحتوى قبل التنفيذ" ولم تُحسم بعد. القيم أدناه اجتهاد مؤقت معقول
// (بطارية 3 أقسام: يُفقد القسم الأول عند 10% أخطاء، الثاني عند 25%، والبطارية
// تنفرغ بالكامل عند 50% كما ورد صراحة بالوثيقة كعتبة نهائية). عدِّل هذا الملف
// فقط عند حسم الفريق للأرقام النهائية — لا حاجة لأي تعديل بنيوي بالكود.

return [
    'battery' => [
        // كل نسبة أخطاء (wrong_count / total_questions) تُفقِد قسمًا واحدًا من 3
        'segment_1_lost_at_wrong_ratio' => 0.10,
        'segment_2_lost_at_wrong_ratio' => 0.25,
        // عند هذه النسبة تنفرغ البطارية بالكامل (القسم الثالث والأخير) ويتوقف الدرس
        'depleted_at_wrong_ratio' => 0.50,
        'total_segments' => 3,
        'recharge_minutes' => 15,
    ],

    'stars' => [
        // نسب مبنية على الإجابات الصحيحة من "المحاولة الأولى فقط" (attempt_number === 1)
        'three_star_min_ratio' => 0.90,
        'two_star_min_ratio' => 0.70,
    ],

    'points' => [
        // نقطة واحدة لكل إجابة صحيحة، وفقط بالمحاولة الأولى (attempt_number === 1)
        'per_correct_answer' => 1,
    ],
];
